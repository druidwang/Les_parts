@using com.Sconit.Entity.ISI
@using com.Sconit.Web.Models.SearchModels.TMS
<fieldset>
    @using (Html.BeginForm("List", "TransportAddress"))
    {
        <div class="search">
            <div class="search-label">
                <label for="Code">
                    @Resources.TMS.TransportAddress.TransportAddress_Code
                </label>
            </div>
            <div class="search-field">
                @Html.TextBox("Code", TempData["TransportAddressSearchModel"] != null ? ((TransportAddressSearchModel)TempData["TransportAddressSearchModel"]).Code : string.Empty)
            </div>

            <div class="search-label">
                <label for="Description">
                    @Resources.TMS.TransportAddress.TransportAddress_Description
                </label>
            </div>
            <div class="search-field">
                @Html.TextBox("Description", TempData["TransportAddressSearchModel"] != null ? ((TransportAddressSearchModel)TempData["TransportAddressSearchModel"]).Description : string.Empty)
            </div>
            <div class="search-label">
            </div>
            <div class="search-field">
            </div>
        </div>
        <div class="search-footer">
            @Html.Hidden("isFromList", true)
            <button type="submit" id="btnSearch">
                @Resources.SYS.Global.Button_Search
            </button>
            <button type="button" id="btnNew" onclick="location.href='@Url.Content("~/TransportAddress/New")'">
                @Resources.SYS.Global.Button_New
            </button>
        </div>
    }
</fieldset>
